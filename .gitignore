# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
.obsidian/

# Claude Code local data (settings, worktrees, cache)
# Ignora tutto tranne la cartella memory (committata per persistere tra sessioni)
.claude/settings.json
.claude/settings.local.json
.claude/worktrees/
.claude/cache/
.claude/todos/
.claude/statsig/

# Dipendenze Node (mai tracciare)
node_modules/

# Python package manager binaries (uv)
uv.exe
uvw.exe
uvx.exe

# Materiale didattico riservato (slide docenti) - solo locale, mai su repo pubblico
**/pdf-fonte/

# HTML intermedio di build (derivato da build_pdf.py, non serve tracciarlo)
archivio/dispensa-politica-economica/DISPENSA_COMPLETA.html
